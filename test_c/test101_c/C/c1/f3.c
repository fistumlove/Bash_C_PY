#include <stdio.h>
#include <math.h>
#include <string.h>
int main()
{
    char name[] = "Fistum G Mitiku";
    printf("Hello %s!\n", name);
    int len = strlen(name);
    printf("The length of your name is %d\n", len);
    int id = pow(4, 4);
    printf("Your id is %d\n", id);
}